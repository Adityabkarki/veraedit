'use client'

/**
 * @deprecated Effects now open in the right panel — see EffectsRightPanel.
 * Kept as a no-op wrapper so existing imports do not break.
 */

interface EffectsDrawerProps {
  projectId?: string
}

export function EffectsDrawer(_props: EffectsDrawerProps = {}) {
  return null
}
